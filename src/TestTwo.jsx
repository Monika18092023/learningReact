import { useState } from "react";

const TestTwo = () => {
     const list=products.map((product)=>{
        return <li style={{color:product.isFruit?'green':'pink'}}>{product.title}</li>
     })

  return (
    <div>
  
        
    </div>
  
  )
};
export default TestTwo;
